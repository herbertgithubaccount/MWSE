return {
	type = "value",
	description = [[When bufferlen is set, it has to be the index of the last element of buffer.

!!! info
	This field is used internally by the implementation and is usually not of interest to the end user.]],
	valuetype = "integer|nil",
}
