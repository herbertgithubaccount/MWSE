return {
	type = "value",
	description = [[Used to store the strings for the result so they can be concatenated at once. When it isn't given, the encode function will create it temporarily and will return the concatenated result.

!!! info
	This field is used internally by the implementation and is usually not of interest to the end user.]],
	valuetype = "string[]|nil",
}
